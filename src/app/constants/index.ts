
interface Constants {
    AVAILABLE_DAYS: number
    hibobToken: string
}

const AVAILABLE_DAYS = 20
const hibobToken = ''


export const constants: Readonly<Constants> = Object.freeze({
    AVAILABLE_DAYS,
    hibobToken,
})
